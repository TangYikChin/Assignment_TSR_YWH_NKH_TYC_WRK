<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignment_tsr_ywh_nkh_tyc_wrk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ElectronicDeviceActivity"
            android:parentActivityName=".ShopListActivity"></activity>
        <activity android:name=".HealthCareActivity"
            android:parentActivityName=".ShopListActivity"/>
        <activity android:name=".ClothedActivity"
            android:parentActivityName=".ShopListActivity"/>
        <activity android:name=".ShopListActivity" />
        <activity android:name=".Login" />
        <activity android:name=".Register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".limited_class"
            android:parentActivityName=".ShopListActivity"/>
    </application>

</manifest>